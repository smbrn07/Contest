#include "bits/stdc++.h"

using namespace std;

typedef long long ll;

#define endl '\n'
#define ff first
#define ss second
#define pb push_back
#define sz(a) int(a.size())
#define allin(a) begin(a), end(a)

const int mod = 1e9 + 7;
const int nmax = 1e5 + 7;

int l1, l2, l3, c1, c2, c3, n, s, f, a[nmax];

signed main() {
   cin.tie(nullptr)->sync_with_stdio(false);
   cin >> l1 >> l2 >> l3 >> c1 >> c2 >> c3;
   cin >> n >> s >> f;
   for (int i = 2; i <= n; ++i) cin >> a[i];

   return 0;
}
